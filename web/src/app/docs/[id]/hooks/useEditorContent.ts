// Compatibility export for focused hook tests and downstream imports.
// New editor composition uses the buffer name defined by the architecture.
export { useEditorBuffer as useEditorContent } from "./useEditorBuffer";
