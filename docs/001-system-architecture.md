# 系统架构与功能索引

## 1. 文档目的

MNote 是一个由 Go 后端和 Next.js 前端组成的自托管笔记系统。本文件记录系统边界、分层方式、页面与能力映射，以及修改代码时必须保持的架构约束。它是正式实现文档的入口，但本文本身已包含理解整体系统所需的信息，不依赖其他文档。

## 2. 系统组成

### 2.1 前端

- `web/` 使用 Next.js App Router、React、TypeScript 和 Tailwind CSS。
- 页面组件负责路由和页面状态，通用交互组件位于 `web/src/components/`。
- `web/src/lib/api.ts` 统一发送 `/api/v1` 请求、注入 Bearer Token、解析业务响应信封并处理登录失效。
- 登录令牌和当前邮箱存储在浏览器 `localStorage`。服务端渲染代码不能直接依赖这些值，鉴权页面在客户端挂载后完成登录检查。
- 编辑器以 CodeMirror 为源码编辑核心，以 ReactMarkdown 为预览核心。源码、预览、草稿和远端保存各自有明确的同步边界。

### 2.2 后端

- `cmd/mnote` 是 HTTP 服务入口，负责加载配置、数据库、存储、Embedding Provider、定时任务和 Gin 路由。
- `internal/handler` 负责 HTTP 入参、鉴权上下文、响应转换，不承载数据一致性规则。
- `internal/service` 负责业务流程、事务边界、并发控制和跨仓储协调。
- `internal/repo` 负责 PostgreSQL 查询，所有用户数据查询必须带用户范围。
- `internal/model` 定义持久化和服务层使用的数据结构。
- `internal/pkg` 提供 JWT、业务错误、邮件和通用基础能力；Embedding Provider 位于 `internal/ai`。
- `internal/db/migrations` 存放嵌入二进制的 SQL 迁移；服务启动时在迁移锁保护下执行。

文档业务对外仍由一个 `DocumentService` 组合，但实现按用途拆分为 core 查询、
`document_write_service.go`、`document_version_service.go`、`document_share_service.go` 和
`document_overview_service.go`。Overview 只聚合首页所需的 recent、tag counts 和总量，不保存文档
内容摘要。Template、Import 和 Handler 只依赖各自使用的窄接口；禁止重新
引入运行期 setter、nil transaction fallback 或让一个 Handler 依赖全部文档能力。

### 2.3 数据与外部依赖

- PostgreSQL 是唯一业务事实源。
- `pgvector` 保存文档分块向量，支持语义搜索；`pgcrypto` 支持数据库侧随机能力。
- 文件可保存在本地目录或 S3 兼容对象存储中，数据库中的资产记录负责建立用户归属和文档引用关系。
- Embedding 通过可配置 Provider 组提供，只用于异步索引、语义搜索和相似文档。Provider 不可用时，
  基础笔记功能仍应正常运行。
- 前端开发服务器只负责页面和静态资源；业务请求直接访问配置的后端地址。

## 3. 页面与功能映射

| 页面 | 路由 | 核心能力 |
| --- | --- | --- |
| 入口 | `/` | 根据本地令牌跳转登录页或文档页 |
| 登录 | `/login` | 邮箱密码登录、OAuth 登录、系统属性展示 |
| 注册 | `/register` | 邮箱验证码注册、注册开关约束 |
| OAuth 回调 | `/oauth/callback` | Provider 回调码换取一次性交换码并完成登录 |
| 文档库 | `/docs` | 文档分页、搜索、筛选、置顶、收藏、分享列表、创建、导入导出 |
| 双栏编辑器 | `/docs/[id]` | Markdown 编辑、预览、保存、草稿、标签、分享、语义相似文档、关联文档 |
| 版本恢复 | `/docs/[id]/revert` | 历史版本对比、差异导航、恢复确认 |
| 标签 | `/tags` | 标签汇总、搜索、分页和删除 |
| 待办 | `/todos` | 无限月份日历、按日查看、待办增删改和完成状态 |
| 模板 | `/templates` | 模板维护、变量解析、基于模板创建文档 |
| 资产 | `/assets` | 上传资产索引、引用文档查询、资源预览 |
| 设置 | `/settings` | 修改密码、OAuth 绑定和解绑 |
| 公开分享 | `/share/[token]` | 分享校验、Markdown 阅读、下载、评论和回复 |
| 编辑器开发页 | `/test-editor` | 隔离验证编辑器输入与交互 |
| Markdown 开发页 | `/test-markdown` | 隔离验证渲染扩展和代码块 |

## 4. 核心业务链路

### 4.1 鉴权链路

1. 用户通过密码或 OAuth 获得 JWT。
2. 前端保存 JWT，并由统一 API 客户端写入 `Authorization: Bearer`。
3. 后端鉴权中间件验证 JWT，把用户标识写入请求上下文。
4. Handler 从上下文取用户标识，Service 和 Repository 按用户范围访问数据。
5. 业务未授权码返回时，前端清理本地会话并跳转登录页。

### 4.2 文档写入链路

1. 编辑器维护本地内容、远端已确认内容和递增保存序号。
2. 保存队列保证同一页面只有一个在途写入，并只保留最新待保存内容。
3. 后端在事务内锁定文档，只有更大的保存序号可以更新内容。
4. 同一事务写入文档版本、标签变更、文档链接、资产引用，并把向量状态标记为待处理。
5. 后台任务异步生成原文分块和向量；保存成功不依赖 Embedding Provider 完成。

### 4.3 公开分享链路

1. 文档所有者创建或更新唯一有效分享配置。
2. 访问者用随机 Token 打开公开页面；后端检查启用状态、过期时间和可选密码。
3. 阅读权限与评论权限分开处理，下载还受独立开关控制。
4. 评论接口允许匿名身份，也可识别有效 JWT 对应的登录用户。

## 5. 架构不变量

- 用户资源隔离必须在后端查询条件中实现，不能依赖前端不展示其他用户数据。
- 文档正文、版本、标签关系、链接关系、资产关系和向量待处理状态必须在一个事务中保持一致。
- Embedding、邮件、对象存储等可选依赖不可阻断与它们无关的基础页面。
- 公开分享接口不能复用需要登录的文档详情接口；分享 Token 是公开访问的唯一资源范围。
- 前端对异步写入必须处理重复触发、过期响应、失败回滚或可恢复草稿。
- 正式行为发生变化时，代码修改必须同步更新对应 `docs/` 文档。
- 代码、测试和正式文档只能依赖稳定实现，不得依赖临时评审材料。

## 6. 修改影响判断

修改前应先判断改动属于哪一条业务链路。涉及下列情况时不能只改页面：

- 修改文档保存字段时，同时检查保存序号、历史版本、标签关系、链接提取、资产提取和向量失效。
- 修改登录状态时，同时检查首页跳转、统一 API 客户端、OAuth 回调和设置页。
- 修改 Markdown 语法时，同时检查编辑器预览、公开分享、导出和历史版本展示。
- 修改文件 URL 或存储 Key 时，同时检查上传、Markdown 引用提取、公开读取和资产索引。
- 修改数据库结构时，同时提供可重复执行的迁移、旧数据处理和后台任务兼容方案。

## 7. 验证基线

- 前后端静态检查、类型检查和单元测试通过。
- 关键页面可以在真实后端下完成登录、创建、编辑、保存、刷新和再次读取。
- 公开分享在未登录、密码、过期、评论和下载组合下行为一致。
- 后台任务失败不会破坏主数据，重试后可以继续处理。
- 数据迁移在空数据库和已有数据库上都能安全启动。
